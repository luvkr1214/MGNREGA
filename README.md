# Backend (mgnrega-insights-backend)

1. Install:

2. Initialize DB (creates sqlite file):

3. Start server:

Server runs on http://localhost:4000

Endpoints:
- GET /api/districts
- GET /api/performance?district=DistrictName&month=YYYY-MM
- GET /api/reverse-geocode?lat=...&lon=...
